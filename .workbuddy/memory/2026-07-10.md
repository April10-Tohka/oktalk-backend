# 2026-07-10

## 端到端联调验证 P0/P1/P2（真实 LLM 驱动 Agent 层）

- 环境约束（用户确认）：ASR/LLM/TTS 额度正常可用；VAD 未启动 → 语音管道会在
  `free_talk.go:281 StreamingVAD` 处报错，`Run()` 在 line 286 `return err` 干净退出
  （不 panic、不泄漏 goroutine，符合预期）；科大讯飞语音评测未续费 → `evalProvider` 为 nil。
- 新增 `internal/service/agent_e2e_test.go`：用真实 Qwen LLM（qwen3.6-flash）驱动 Agent 编排层，
  验证 P0/P1/P2 生成代码本身（绕开 VAD/讯飞基础设施）。
  - TestE2E_P0_PlainTextResponse：纯文本轮产出 IsDone:true + 非空回复（P0 不变量 OK）。
  - TestE2E_P1_ToolCallByLLM：真实 LLM 自主调用 update_user_profile → 长期记忆写入 store
    （Name=小明 AgeGroup=6-8 Topics=[恐龙 太空]）。
    **重要发现（已记入 MEMORY.md）**：弱提示下模型会"假装记住"（回复"记在小本本上啦"）但**不实际调工具**，
    记忆静默未持久化；改用强约束提示（"先调工具再回复，否则等于欺骗小朋友"）后稳定触发。写记忆依赖模型合规性。
  - TestE2E_P1P2_RecallViaTool：新会话强制调 get_user_profile 才回答 → 回复含"小明"，
    证明 ReAct 工具结果回灌 + P2 读路径端到端 OK。
  - TestE2E_P2_NilSafeToolRegistration：evalProvider=nil（讯飞不可用）时仅注册记忆工具、
    异步评测工具不注册；给桩 evalProvider 后 assess_pronunciation 正常注册（无需真实讯飞）。
  - TestE2E_P2_ControlHandoverStateMachine：armFollowRead→consumeFollowRead(命中并清除)
    →二次 consume 未命中，状态机正确。
- 全部 5 个 e2e 测试通过；`go test ./internal/service/` 整体全绿，无回归。`go build ./...` 通过；
  `go vet ./...` 仍仅预先存在的 tts/aliyun 一处无关告警。
