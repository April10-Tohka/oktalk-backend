# 2026-07-11 工作日志

## P3/P4 完善项落地（原 P3 主动规划 + P4 打磨）

### 1. Registry 稳定性/安全三防护（`internal/agent/tools.go`）
- 新增 `allowList`（儿童安全白名单）、`budget`/`used`（跨会话工具调用预算）、`toolTimeout`（单次执行超时）。
- `Execute` 顺序：白名单拦截 → 预算拦截（耗尽返回 "budget exhausted" 提示强制收尾） → `context.WithTimeout` 包裹工具执行。
- 新增方法 `AllowTools/SetToolTimeout/SetBudget/ToolCallsUsed`。

### 2. Agent 单轮 LLM 超时（`internal/agent/agent.go`）
- 常量 `turnTimeout=2min`，每步 `ChatWithToolsStream` 包裹 `stepCtx`；`stepCancel` 存于 `reactLoopDecoder.stepCancel`，在 `endStep()` 释放（**不用 defer**，避免首个文本事件后误取消）。
- `Close()` 同步关闭 stepStream + cancel。

### 3. 会话级目标 `GoalState`（P3 多轮目标规划，`internal/service/free_talk.go`）
- Session 增加 `goalState *GoalState` + `goalMu`；方法 `setGoal/updateGoalProgress/goalContextNote`。
- `llmGoroutine` 每轮（含静默触发）复制 messages 切片后注入 `goalContextNote()`（不污染持久化历史）；目标未完成时提示 LLM 主动推进。

### 4. 目标管理工具（`internal/service/agent_tools.go`）
- 新增 `set_session_goal` / `update_goal_progress`，在 `registerAgentTools` 始终注册（无外部依赖）。

### 5. 儿童安全 + 参数校验（`conversation_memory.go` / `agent_tools.go`）
- `buildSystemPrompt` 新增 "Child-safety guardrails" 小节（仅白名单工具、不联网/不执行代码、内容儿童友好）。
- `start_pronunciation_practice` 增加 `unit_id` 存在性校验（无效返回安全提示）。

### 6. Run() 串联（`internal/service/free_talk.go`）
- 创建 registry 后：`SetToolTimeout(15s)` / `SetBudget(50)` / `AllowTools(8个安全工具名)`。

### 验证
- `go build ./...` 全绿；`go vet` 仅余预先存在的 `tts/aliyun` 无关告警。
- 新增单测：`internal/agent/registry_test.go`（预算耗尽/超时/白名单拒绝/无白名单放行）、`internal/service/goal_state_test.go`（GoalState 方法）。
- 真实 LLM e2e：新增 `TestE2E_P3_GoalPlanningByLLM`（set_session_goal 设定"学习三个动物单词"+ update_goal_progress 记录"学了单词 dog"，均经真实 qwen 触发）。
- `internal/agent` + `internal/service` 全包测试通过，无回归。
